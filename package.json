{
  "name": "customer-time-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone local app for tracking time on customer projects.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
